name: Playwright Tests
on:
  push:
    branches: [ main ]
jobs:
  playwrighttests:
    runs-on: ubuntu-latest
    steps:
      - name: Check repository
        uses: actions/checkout@v2
      - name: Say hello
        run: echo hello
